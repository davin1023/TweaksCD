-- ============================================================================
-- TweaksUI: Cooldowns - RadialSwipe (REMOVED)
-- Radial swipe functionality has been removed due to Midnight API restrictions.
-- This stub exists only to safely handle any leftover references.
-- ============================================================================

local addonName, TUICD = ...

-- Empty stub so any code referencing TUICD.RadialSwipe won't error
TUICD.RadialSwipe = {
    InitializeRadialSwipe = function() end,
    OnUpdate = function() end,
    CreateSpinner = function() return nil end,
}
